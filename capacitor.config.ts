import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'jkc.modixom.com',
  appName: 'modixom',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;